# Copy to .env and fill in real values. Do NOT commit .env.
# Used by main.py / deployment only — the classifier itself has no secrets.

# Port for the FastAPI server (uvicorn)
PORT=8000

# Bind host (0.0.0.0 for container/VM, 127.0.0.1 for local-only)
HOST=0.0.0.0
